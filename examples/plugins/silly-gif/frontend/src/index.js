// Empty bootstrap entry for the Module Federation remote. The useful output is
// `remoteEntry.js` (the container exposing ./MapOverlay and ./MarkerField), which webpack
// emits from the ModuleFederationPlugin regardless of this entry. Nothing runs here.
export {};
