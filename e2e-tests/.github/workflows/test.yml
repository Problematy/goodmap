on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:
  e2e-tests:
    uses: ./.github/workflows/test-base.yml
    with:
      goodmap-repository: 'problematy/goodmap'
      goodmap-version: 'main'
      goodmap-frontend-repository: 'problematy/goodmap-frontend'
      goodmap-frontend-version: 'main'
